auth.sh

# ~/.config is symlinked to this directory, so apps write their state/secrets
# straight into the repo. Keep secrets and app-managed state out of git.
#
# Secrets live in ~/.local/secrets and are symlinked in by symlink.py.
.config/autostore/
.config/cursor/

# Machine-specific / app-managed state (not worth versioning)
.config/jgit/

# Zed (M33): settings.json is the tracked source symlink.py deploys;
# everything else it writes into this dir (workspace/session state) isn't.
.config/zed/*
!.config/zed/settings.json

# Install-target symlinks created by symlink.py (real source lives at nvim/).
# Note: .config/starship.toml is tracked on purpose (it is the install symlink).
.config/nvim/

# Editor backups
.config/zellij/*.bak
