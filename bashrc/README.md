#.bashrc Repository

This repository contains configuration files for my shell environment. This is run either in git bash or WSL (Ubuntu), and need to _somewhat_ support both.

## Purpose

The purpose of this repository is to store and manage my Bash configuration so that I can easily set up new environments or recover existing ones.
