# Linux distro provisioning scripts

Per-distro package installation, run manually (not by `bootstrap.sh` — see the root
`README.md` for why):

- `ubuntu.sh`
- `debian.sh`
- `arch.sh`
- `opensuse.sh`
