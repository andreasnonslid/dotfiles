# SSH setup scripts

- **setup_ssh_key.sh** - Create, delete, rename, or list SSH keys; sets git user.name/email on create
- **setup_AS_git.sh** - Add AutoStore git host entry to ~/.ssh/config for @autostoresystem.com keys
